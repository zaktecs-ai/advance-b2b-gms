# Advance B2B GMS — runtime dependencies.
# Python 3.11+ (the package's requires-python floor; see pyproject.toml).
#
# Upper bounds prevent a silent major-version break; a lockfile
# (requirements.lock, generated by `pip freeze`) is recommended for
# byte-reproducible deploys. Test-only packages (pytest) live in
# pyproject.toml's [project.optional-dependencies].test — NOT here (E4).

setuptools>=61.0
playwright>=1.40,<2
httpx>=0.25,<1
beautifulsoup4>=4.12,<5
lxml>=4.9,<7
PyYAML>=6.0,<7
python-dotenv>=1.0,<2
dnspython>=2.4,<3
psutil>=5.9,<8
openpyxl>=3.1,<4
pydantic>=2.5,<3
phonenumbers>=8.13,<10
ftfy>=6.2,<7

# -- optional, enables richer tech detection (falls back to regex if absent)
wappalyzer-python3>=0.0.11,<1

# A byte-reproducible deploy should install from requirements.lock
# (generated via `pip freeze > requirements.lock` from a known-good venv).
